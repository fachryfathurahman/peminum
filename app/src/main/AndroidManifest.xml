<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kelsiraman.peminum">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".mainlayout.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SplashScreen">

        </activity>
        <activity android:name=".onboardingscreen.onBoardingActivity" />
        <activity android:name=".pendaftaran.SleepTimeActivity" />
        <activity android:name=".pendaftaran.WakeUpTimeActivity" />
        <activity android:name=".pendaftaran.WeightActivity" />
        <activity android:name=".pendaftaran.GenderActivity" />
        <activity android:name=".SignUp" />
        <activity android:name=".loginActivity" />
    </application>

</manifest>